see mitsi's page scans and the effect on the tesseract global threshold choice, ie the in page pictures are negatively impacting the binarization stage. and that, consequently, negatively impacts segmentation and possibly the OCR activity itself -- though LSTM OEM 1 uses the original source image pixels for its scanlines' feature vector.
